using graphBackend.Models;

namespace graphBackendTests
{
    [TestClass]
    public sealed class TestLoaner
    {
        //[TestMethod]
        //public async Task TestGetAllLoaners()
        //{
        //    var LoanerRepo = new graphBackend.Repositories.LoanerRepositoryGraph();
        //    List<Loaner> loaners = await LoanerRepo.GetAllLoaner();
        //    Assert.AreEqual("Mads", loaners.Find(l => l.Id == 1)?.FirstName);
        //}
        //[TestMethod]
        //public async Task TestGetLoanerById()
        //{
        //    var LoanerRepo = new graphBackend.Repositories.LoanerRepositoryGraph();
        //    Loaner? loaner = await LoanerRepo.GetLoanerById(1);
        //    Assert.IsNotNull(loaner);
        //    Assert.AreEqual("Mads", loaner.FirstName);
        //}
    }
}
